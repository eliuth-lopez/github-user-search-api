# github-user-search-api
A Node.js backend service that translates natural language queries into structured GitHub Search API requests. It acts as a proxy to discover users based on criteria like location, language, and followers.
